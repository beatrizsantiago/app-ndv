import React, { useEffect } from 'react'
import { Text } from 'react-native'

import { ContainerCenter } from './styles/MainStyled'

export default SendFeedback = ({ navigation }) => {

    return (
        <ContainerCenter>
            <Text>SendFeedback</Text>
        </ContainerCenter>
    )
}
